include head.pug
link(rel='stylesheet', type='text/css', href='/css/users.css' media='screen')

.container
    .user.col-md-4.col-md-offset-3.border
        p ID:           #{all.dataValues.id}
        p First Name:   #{all.dataValues.firstName}
        p Last Name:    #{all.dataValues.lastName}
        p Username:     #{all.dataValues.username}